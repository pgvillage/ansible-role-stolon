export PGSERVICE=local
for P in /etc/sysconfig/stolon-stsentinel-*; do
    F=$(basename "$P")
    export STOLONCTL_CLUSTER_NAME=${F#stolon-stsentinel-}
    eval $(sed -e '/#/d;s/^STSENTINEL/export STOLONCTL/' "$P" )
    /usr/local/bin/stolonctl status | tail -11
done
